import {
  ProtectionRuleParams,
  ProtectionRuleId,
} from '@modules/protection-rules';

export class ProtectionRuleParamsMock {
  static readonly protectionRuleParamsMock: ProtectionRuleParams = {
    id: ProtectionRuleId.AC_OUTPUT_VOLTAGE,
  };
}
